Types

numbers
booleans
null
strings

Functionality

number unary negation (- not)
number binary arithmetic (+ - * /)
number binary comparison (<= >= < > equals)

boolean operators (not and or)

string operations (+ equals)

expression grouping (parentheses)
expression comparison (equals)

output (print)

reading variables and assignment (=)
local variables and scopes ({})

control flow (if else while for)

global functions (function (...params) {body})
native functions (clock)

closures (function (...params) { function (...params) {body} body})

class blueprint with methods (class { method() {}})
instances (a = A())
constructors

runtime errors